# For building the docs site locally:  (cd docs && bundle install && bundle exec jekyll serve)
# CI uses actions/jekyll-build-pages (the github-pages gem), so this file is
# excluded from the published site via _config.yml.
source "https://rubygems.org"

gem "jekyll", "~> 4.3"
gem "just-the-docs"
gem "jekyll-relative-links"
gem "jekyll-seo-tag"
